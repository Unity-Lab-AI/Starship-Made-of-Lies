# Build outputs
bin/
obj/

# IDE
.vs/
*.user
*.suo

# MDK2 intermediates
intermediate-script*.cs
symbol.map

# NuGet
*.nupkg

# OS files
.DS_Store
Thumbs.db

# SMoL build outputs (additive 2026-05-09 per PHASE 0.11 policy — UMS entries above PRESERVED until PHASE 15)
node_modules/
dist/
build/
*.tsbuildinfo
.vite/
.pnpm-store/
.eslintcache
.prettiercache
*.log
.env
.env.local
.env.*.local
src-tauri/target/
src-tauri/Cargo.lock

# Proprietary Unity AI Lab .claude/ workflow system — public repo, NEVER commit
.claude/

# Local dev infrastructure scratch (cloudflared tunnel config — never commit, may contain tokens)
config.yml
local-server/config.yml

# Runtime server data — FileAccountStore writes accounts.json + Colyseus FileSnapshotStore
# writes per-room snapshots here. Never commit player PII / OAuth ids into the repo.
data/
