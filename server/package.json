{
  "name": "@smol/server",
  "version": "0.0.0",
  "description": "SMoL multiplayer + AI backend (TypeScript). Authoritative game state, fog-of-war enforcement, AI civ background processes, lobby. WebSocket framework (Colyseus or custom) added in PHASE 10 per no-half-implementations LAW.",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --check .",
    "format:write": "prettier --write ."
  }
}
